<%= form_for(@attribute_group, url: {action: (params[:action] == 'new_attribute_group' ? 'create_attribute_group' : 'update_attribute_group')}) do |f| %>
  <% if @attribute_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute_group.errors.count, "error") %> prohibited this attribute_group from being saved:</h2>

      <ul>
      <% @attribute_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
  <div class="field" style='display: none;'>
    <%= f.label :name %><br>
    <%= f.hidden_field :category_id %>
  </div>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
