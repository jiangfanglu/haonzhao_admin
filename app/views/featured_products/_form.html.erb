<%= form_for(@featured_product) do |f| %>
  <% if @featured_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@featured_product.errors.count, "error") %> prohibited this featured_product from being saved:</h2>

      <ul>
      <% @featured_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id, :class=>"form-control" %>
  </div>
  <div class="field">
    <%= f.label :store_id %><br>
    <%= f.number_field :store_id, :class=>"form-control", :value=>0 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
