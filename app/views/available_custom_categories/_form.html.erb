<%= form_for(@available_custom_category) do |f| %>
  <% if @available_custom_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@available_custom_category.errors.count, "error") %> prohibited this available_custom_category from being saved:</h2>

      <ul>
      <% @available_custom_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.number_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :sort_order %><br>
    <%= f.number_field :sort_order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
